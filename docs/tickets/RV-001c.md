---
id: RV-001c
title: "TariffEvaluator: fix partial-period overbilling in finalize()"
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-tariff/src/evaluator.rs
references:
  - docs/review-findings.md
verification:
  - cargo test -p hares-tariff
  - cargo clippy -p hares-tariff -- -D warnings
---

## Status: Complete

## Work Done

- [x] Changed `finalize()` signature to `finalize(&mut self, sim_end: DateTime<Tz>)`
- [x] Fixed charge proration: `elapsed_days = (min(sim_end, period_end) - period_start).num_days()`
- [x] Summary `end` field uses `actual_end` (clamped to period end) instead of always `period_end`
- [x] Double-call guard preserved: `reset()` after first call, second call returns `None`
- [x] Updated all callers (existing test, no other Rust callers)

## Tests Added

- [x] `evaluator_finalize_partial_period_prorates_fixed_charge` — 15 days of a 31-day period at $1/day → asserts $15 fixed charge
- [x] `evaluator_finalize_full_period_unchanged` — full period at $1/day → asserts $31 fixed charge (regression guard)
- [x] `evaluator_finalize_double_call_returns_none` — existing test updated with `sim_end` parameter

## Verification

- [x] `cargo test -p hares-tariff` — 102 passed
- [x] `cargo clippy -p hares-tariff -- -D warnings` — clean
