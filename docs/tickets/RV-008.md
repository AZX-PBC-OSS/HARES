---
id: RV-008
title: "~~Dwelling step loop alloc cleanup~~ (subsumed by RV-005)"
kind: refactor
depends_on: []
files_to_touch: []
references:
  - docs/tickets/RV-005.md
verification: []
---

## Status: Subsumed by RV-005

This ticket's work items (T2-5 StepResult clone, T2-6 thermal_update clone, T2-8 timestamp allocation) have been merged into RV-005 to avoid concurrent modifications to `dwelling/mod.rs`. See RV-005 for the consolidated work and test plan.

## Completion Notes (session 2026-03-27)

- **T2-6 (thermal_update clone):** Complete. `apply_thermal_update_to_zones` called before `upsert_domain(thermal_update)` move. Clone retained only under `#[cfg(feature = "observe")]`. Tracked in RV-005.
- **T2-8 (timestamp allocation):** Complete. Pre-allocated `timestamp_buf` String field, formatted in-place via `write!` with `%+`. Tracked in RV-005.
- **T2-5 (StepResult clone):** Structurally unavoidable. `step()` must both return `Result<StepResult>` to the caller and store it in `simulation_results.steps` — both need ownership with no safe borrow-then-move ordering. Documented with a comment in source. Tracked in RV-005.
