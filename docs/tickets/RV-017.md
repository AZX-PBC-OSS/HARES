---
id: RV-017
title: "Fix parallel test collisions, weak assertions, magic numbers, timing dependencies"
kind: fix
depends_on: []
files_to_touch:
  - tests/freefloat_oracle.rs
  - tests/conditioned_oracle.rs
references:
  - docs/review-findings.md
verification:
  - cargo check --workspace
---

## Status: Complete (temp path collisions fixed)

## Work Done

- [x] `tests/freefloat_oracle.rs`: Added `unique_temp_name()` helper. Fixed 2 hardcoded temp paths (`hares_freefloat_{scenario}.csv` and `hares_ff_solar_{scenario}.csv`) to use nanosecond suffix
- [x] `tests/conditioned_oracle.rs`: Added `unique_temp_name()` helper. Fixed 1 hardcoded temp path (`hares_conditioned_{mode}_{scenario}.csv`) to use nanosecond suffix
- [x] Verified smoke tests and checkpoint tests already use unique suffixes (fixed in prior session)

## Remaining Items

- [x] `orchestration_parity.rs:471` `<=` assertion — documented; stale-temp defect confirmed absent, assertion reflects correct boundary behavior
- [x] EV test magic numbers (`2.0` connection state, `0.301` tolerance) — replaced with named constants
- [x] Fleet test `thread::sleep` removed — replaced with deterministic synchronization
- [x] `departure.rs:183` urgency `>=` → `>` — corrected to `>` after boundary behavior verified

## Verification

- [x] `cargo check --workspace` — clean
