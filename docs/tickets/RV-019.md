---
id: RV-019
title: "Code quality: replace sentinels, add try_save_postcard, guard zero time_res"
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-core/src/dwelling/mod.rs
  - crates/hares-equipment/src/lib.rs
  - crates/hares-core/src/actors/ev_driver/mod.rs
references:
  - docs/review-findings.md
verification:
  - cargo clippy -p hares-core -p hares-equipment -- -D warnings
  - cargo test -p hares-core --lib
---

## Status: Complete

## Work Done

- [x] **T7-1:** Replaced `usize::MAX` sentinel in `zone_env_indices` with `Option<usize>`. Updated struct field type (`Vec<usize>` → `Vec<Option<usize>>`) and usage site to `if let Some(env_idx)`. Compiler-enforced — no future access can forget to check.
- [x] **T7-4:** Added `pub fn try_save_postcard<T: Serialize>(state: &T) -> Result<Vec<u8>>` alongside existing panicking `save_postcard`. Existing call sites unchanged — `try_save_postcard` is available for `DwellingCheckpoint::save()` and similar paths where graceful failure matters.
- [x] **T7-9:** Added `debug_assert!(res_seconds > 0)` and `.max(1)` clamp in `EvDriverActor::decide()` to prevent `f64::INFINITY / 0.0 → u32::MAX` trip steps.
- [x] **T7-6:** InvariantChecker as Dwelling field — deferred to RV-005 (already noted in ticket).
- [x] **T7-7:** Clippy errors in bms.rs/ev_driver — verified clean. Pre-existing issues were resolved in prior session refactoring.

## Tests

- [x] 308 hares-core lib tests pass — `zone_env_indices` change is exercised by all dwelling construction tests
- [x] Clippy clean on both hares-core and hares-equipment

## Verification

- [x] `cargo clippy -p hares-core -p hares-equipment -- -D warnings` — clean
- [x] `cargo test -p hares-core --lib` — 308 passed
- [x] `cargo check --workspace` — clean
