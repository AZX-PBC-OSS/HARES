---
id: RV-001a
title: "TariffEvaluator: bounds-check accessor methods to prevent OOB panics"
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-tariff/src/evaluator.rs
references:
  - docs/review-findings.md
verification:
  - cargo test -p hares-tariff
  - cargo clippy -p hares-tariff -- -D warnings
---

## Background/Context

`current_price()`, `current_export_price()`, `current_period_name()`, and `tier_multiplier()` index into arrays using `self.step_index` with no bounds check. After simulation completion or when using `advance()` directly (which does not set `self.finished`), `step_index` can exceed array length, causing a panic.

## Design Decision

**Return the last valid value when exhausted, not `Option`.** Changing to `Option<T>` is a breaking API change affecting `dwelling/mod.rs:1685-1686` and `py_dwelling.rs:1208-1210`. Instead: when `step_index >= array.len()`, return the last element (clamped index). This is semantically correct (the final price/period persists after simulation ends) and avoids breaking callers. Add a `pub fn is_finished(&self) -> bool` method for callers that need to check.

## Progress

**Session 2026-03-27:**

- [x] Added `finished: bool` field to `TariffEvaluator`
- [x] `step()` returns `None` when `finished` is true
- [x] `advance()` sets `finished = true` when it returns `false`
- [x] Test added: `evaluator_step_after_end_returns_none`

Still needed:
- [ ] Accessor clamping: `current_price()`, `current_export_price()`, `current_period_name()`, `tier_multiplier()` still index with `step_index` unchecked — will panic if called after `advance()` exhaustion without going through `step()`
- [ ] `pub fn is_finished(&self) -> bool` accessor method

## Work to Do

- [x] Ensure `advance()` sets `self.finished = true` when it returns `false`, matching `step()` behavior
- [ ] Clamp `step_index` to `self.price_array.len() - 1` in all accessor methods (return last valid value when exhausted)
- [ ] Add `pub fn is_finished(&self) -> bool` for callers that need to distinguish active vs exhausted state
- [ ] No call site changes needed (existing callers get last valid value, not a panic)

## Files to Touch

- `crates/hares-tariff/src/evaluator.rs`: Bounds checks on `current_price`, `current_export_price`, `current_period_name`, `tier_multiplier`

## Tests Added

- `accessor_returns_last_value_after_simulation_end` — advance evaluator past final step via `step()`. Assert `current_price()` returns the last valid price (not panic). Assert `is_finished()` returns true
- `accessor_returns_last_value_after_advance_exhaustion` — use `advance()` to exhaust steps without calling `step()`. Assert accessors return last valid values. Assert `is_finished()` returns true
- `accessor_valid_mid_simulation` — assert accessors return correct current values during normal operation (regression guard)
- `accessor_after_step_returns_none` — call `step()` until it returns `None`, then call accessors. Assert no panic and values are the last valid entry

## Measures of Success

- [ ] No panic possible from any public accessor method regardless of evaluator state
- [ ] All existing tariff tests pass with updated return types

## Verification

- [ ] `cargo test -p hares-tariff` passes
- [ ] `cargo clippy -p hares-tariff -- -D warnings` passes
