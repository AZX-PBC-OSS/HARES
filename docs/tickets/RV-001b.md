---
id: RV-001b
title: "TariffEvaluator: fix ratchet drop for non-first demand rates"
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-tariff/src/evaluator.rs
  - crates/hares-tariff/src/billing.rs
references:
  - docs/review-findings.md
verification:
  - cargo test -p hares-tariff
  - cargo clippy -p hares-tariff -- -D warnings
---

## Status: Complete

## Work Done

- [x] Added `pub fn effective_peak_with_ratchet(&self, ratchet: &Option<RatchetConfig>) -> f64` to `BillingState` — allows each demand rate to supply its own ratchet for coincident demand
- [x] Changed `compute_demand_charge()` to use `effective_peak_with_ratchet(&dr.ratchet)` for coincident demand rates (`period_name: None`) instead of the global `effective_peak_kw()`
- [x] Replaced misleading `find_map` global ratchet with `None` — the global `ratchet_config` on `BillingState` is no longer used by the evaluator. Each demand rate's own ratchet is used directly
- [x] Per-rate ratchet for TOU demand rates via `effective_peak_for_period(idx, &dr.ratchet)` was already implemented in prior session

## Tests

- [x] Existing `billing_per_period_ratchet_applied` covers per-rate TOU demand ratchet
- [x] Existing `billing_ratchet_applies` covers global ratchet mechanics via `effective_peak_kw()`
- [x] All 102 tariff tests pass — no regression from removing `find_map`

## Verification

- [x] `cargo test -p hares-tariff` — 102 passed
- [x] `cargo clippy -p hares-tariff -- -D warnings` — clean
