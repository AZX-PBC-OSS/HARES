---
id: CO-011
title: "CI grep guard + final cleanup"
kind: implement
depends_on: [CO-010]
files_to_touch:
  - crates/hares-core/src/dwelling/mod.rs
  - crates/hares-core/src/actors/bms.rs
  - crates/hares-core/src/actors/ev_driver/mod.rs
  - crates/hares-types/src/telemetry_keys.rs
  - docs/tickets/review/AR-001.md
references:
  - ~/.claude/plans/linked-herding-island.md
verification:
  - cargo build
  - cargo test --lib
  - cargo clippy --lib --tests
  - cargo fmt --check
---

## Background/Context

Final enforcement: no string telemetry reads may exist in core simulation paths.
Add a test-based CI guard and clean up any remaining issues.

## Work to Do

- [ ] Add `#[test] fn no_string_telemetry_reads_in_core()` that reads all `.rs` files
      in `crates/hares-core/src/` (excluding `#[cfg(test)]` modules and `mod tests`
      blocks), scans for patterns: `telemetry().get(`, `telemetry.get(`,
      `telem.get(`, `.0.get(` on Telemetry types. Exclude string literals and
      comments from matching. Assert zero hits. Use explicit allowlist comment
      `// allowed:` for any legitimate exception. NOTE: this is a pragmatic first
      step; a proper Rust AST-based lint (e.g., clippy custom lint or
      `syn`-based test) would eliminate false positives/negatives — consider as
      follow-up if the grep guard proves noisy.
- [ ] Audit all remaining `telemetry.get` calls in hares-core/src/ and either:
      - Migrate to core_output reads, or
      - Move to test-only code, or
      - Add `// allowed:` comment with justification
- [ ] Update AR-001.md implementation status section to reflect completed work.
- [ ] Remove any dead telemetry key constants from `telemetry_keys.rs` that are no
      longer referenced (if any).
- [ ] Final `cargo fmt`, `cargo clippy --lib --tests`, `cargo test --lib`.

## Measures of Success

- [ ] CI test enforces no string telemetry reads in core
- [ ] AR-001 ticket is fully updated
- [ ] Codebase is clean: build, clippy, fmt, tests all pass
- [ ] Zero fallback chains remain anywhere in core
