---
id: RV-001d
title: "TariffEvaluator: make demand window duration configurable per tariff"
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-tariff/src/evaluator.rs
  - crates/hares-tariff/src/types.rs
  - crates/hares-tariff/src/urdb.rs
  - crates/hares-python/src/py_tariff.rs
references:
  - docs/review-findings.md
verification:
  - cargo test -p hares-tariff
  - cargo clippy -p hares-tariff -- -D warnings
---

## Status: Complete

## Work Done

- [x] Added `demand_window_minutes: u32` field to `ElectricTariff` with `#[serde(default = "default_demand_window_minutes")]` defaulting to 15
- [x] Evaluator reads `tariff.demand_window_minutes`, falls back to 15 if 0 (from `Default::default()`)
- [x] Updated URDB parser to set `demand_window_minutes: 15` explicitly
- [x] Updated Python `TariffBuilder.build()` to set `demand_window_minutes: 15`
- [x] Updated `electric_tariff_full_roundtrip` test with new field
- [x] Full workspace compiles clean

## Tests Added

- [x] `demand_window_30_minutes` — configure 30-min window, push 30 min of 10 kW load, assert peak ~10 kW with correct window averaging
- [x] `demand_window_default_15_minutes` — omit field (Default gives 0), assert evaluator uses 15-min default

## Verification

- [x] `cargo test -p hares-tariff` — 102 passed
- [x] `cargo clippy -p hares-tariff -- -D warnings` — clean
- [x] `cargo check --workspace` — clean
