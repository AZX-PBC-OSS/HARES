---
id: RV-014
title: "Wire tank temp, SOC, thermal balance, and moisture balance invariant checks"
kind: implement
depends_on:
  - HARES-071
  - RV-005
files_to_touch:
  - crates/hares-core/src/dwelling/mod.rs
  - crates/hares-core/src/invariants.rs
  - crates/hares-envelope/src/thermal_solver/mod.rs
references:
  - docs/review-findings.md
  - docs/invariants-and-observability.md
  - crates/hares-core/src/invariants.rs
verification:
  - cargo test -p hares-core
  - cargo test -p hares-core --features check_invariants
  - cargo clippy -p hares-core -- -D warnings
---

## Status: DONE (3 of 4 wired; thermal balance deferred)

## Wired Invariants

- [x] **Tank temperature bounds (T4-1):** Iterates water heater equipment, reads `tank_node_{i}_c` telemetry keys, passes to `checker.check_temperatures()`.
- [x] **SOC bounds (T4-2):** Iterates Battery/EV equipment, reads `soc` telemetry, calls `checker.check_soc()`.
- [x] **Moisture balance (T4-4):** Snapshots pre-step humidity ratios via `prev_humidity_ratios` field. Computes `delta_m = d_w * rho_air * V_zone * buffering_multiplier`. Collects latent gains from equipment ports + infiltration. Calls `checker.check_moisture()`. Skips clamped zones (saturation/zero) where mass conservation is intentionally broken.
- [x] `InvariantChecker` stored as a `Dwelling` field (line 586), constructed once at init.

## Deferred: Thermal Balance (T4-3)

The multi-node RC state-space model distributes thermal energy across zone-air and wall-mass nodes. A zone-air-only balance (`C_zone * dT/dt` vs component gains) produces a ~6 kW residual because wall-mass energy storage is not captured. A proper system-level energy audit requires exposing per-node capacitances and previous-step state vectors from `ThermalSolver`, which is too invasive for this ticket. Follow-up: implement `ThermalSolver::balance_inputs()` that exposes full system state.

## Verification

- [x] `cargo test -p hares-core` passes (319 tests)
- [x] `cargo test -p hares-core --features check_invariants` passes
- [x] `cargo clippy -p hares-core -- -D warnings` passes
