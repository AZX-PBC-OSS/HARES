---
id: RV-007
title: "ThermalSolver hot-loop optimizations"
kind: refactor
depends_on: []
files_to_touch:
  - crates/hares-envelope/src/thermal_solver/mod.rs
  - crates/hares-envelope/src/thermal_solver/solar.rs
references:
  - docs/review-findings.md
verification:
  - cargo test -p hares-envelope
  - cargo clippy -p hares-envelope -- -D warnings
---

## Background/Context

Four hot-loop performance issues in the thermal solver:

1. **T2-3:** `format_domain_update()` allocates and sorts `zone_temperatures_c` and `latent_pairs` Vecs every step. Zone ordering is fixed after init — no sort needed per step.

2. **T2-7:** `lwr_by_zone_buf` is unconditionally `.clone()`d into `EnvelopeComponentGains` every step. If callers only read by reference, the clone is unnecessary.

3. **T2-9:** 8 separate `.iter().find()` calls over `infiltration_buf` per step to extract different fields from the same entry. Should extract once.

4. **T2-10:** `GlazingCurve::from_u_shgc()` is called per surface per step with constant U-factor and SHGC values. Should be precomputed at init.

## Progress

**Session 2026-03-27:**

- [x] **(T2-7)** `lwr_by_zone_buf`: Changed from `.clone()` to `std::mem::swap` pattern. Zero-copy handoff to `EnvelopeComponentGains`. Capacity is preserved after the swap via `reserve`.
- [x] **(T2-9 partial)** `infiltration_by_zone_buf` pre-allocated as a scratch field on `ThermalSolver`. Each step: `clear()` + `extend()` + `sort_by_key()` reuses existing capacity. Swapped into `component_gains` using the same swap pattern as `lwr_by_zone_buf`. Capacity preserved.

Still needed:
- [ ] **(T2-3)** `format_domain_update()` zone_temps prealloc: pre-populate zone IDs at init, update `f64` values only per step. No per-step sort or Vec allocation.
- [ ] **(T2-9 full)** Consolidate the 8 separate `.iter().find()` calls over `infiltration_buf` per step into a single lookup that binds all needed fields from one reference.

**Note:** GlazingCurve precomputation (T2-10) has been split into RV-007b.

## Work to Do

- [x] `lwr_by_zone_buf`: Use `std::mem::swap` instead of clone to hand off to `EnvelopeComponentGains`. Reserve capacity after swap.
- [x] `infiltration_by_zone_buf`: Pre-allocate as scratch field. Use `clear()` + `extend()` + swap pattern to avoid per-step allocation.
- [ ] `format_domain_update()`: Add `zone_temps_buf: Vec<(ZoneId, f64)>` and `latent_pairs_buf: Vec<(ZoneId, f64)>` as fields on `ThermalSolver`. Pre-populate zone IDs at init in sorted order. Each step, update only the `f64` values via indexed assignment. No sort needed
- [ ] Infiltration lookups: In `build_input_vector`, find the `InfiltrationCoupling` for the indoor zone once. Bind to a local reference, then read `q_infiltration_w`, `q_forced_vent_w`, `q_natural_vent_w`, `q_sensible_diagnostic_w`, and all airflow fields from that single reference

**Note:** GlazingCurve precomputation (T2-10) has been split into RV-007b because window surface properties (shading, dynamic glazing) may change mid-simulation — precomputing assumes immutability which needs separate verification.

## Files to Touch

- `crates/hares-envelope/src/thermal_solver/mod.rs`: Scratch buffer fields, infiltration lookup consolidation, lwr_by_zone borrow pattern
- `crates/hares-envelope/src/thermal_solver/solar.rs`: Precomputed `GlazingCurve` on surface info

## Tests Added

**hares-envelope:**

- `thermal_solver_zone_temps_identical_after_prealloc` — run 48-hour single-zone simulation. Assert zone temperature at every step matches pre-refactor values to f64 precision
- `thermal_solver_multizone_temps_identical` — run 24-hour two-zone simulation. Assert both zones match pre-refactor values
- `thermal_solver_envelope_gains_identical` — compare `EnvelopeComponentGains` fields (window_solar, infiltration, port_sensible, etc.) for 10 representative steps. Assert all values identical
- `glazing_curve_precomputed_matches_per_step` — for 5 representative window configs, assert `GlazingCurve::from_u_shgc()` at init produces identical IAM corrections as the per-step computation at solar noon
- `infiltration_consolidated_lookup_identical` — for a multi-zone config, assert all infiltration-derived values (q_infiltration_w, q_forced_vent_w, etc.) match pre-refactor output

## Measures of Success

- [ ] Zero per-step allocations in `format_domain_update`
- [ ] Zero per-step clones in `lwr_by_zone_buf` path
- [ ] Single buffer traversal for infiltration lookups (down from 8)
- [ ] `GlazingCurve` computed once at init, not per step
- [ ] All envelope oracle tests produce identical numeric results

## Verification

- [ ] `cargo test -p hares-envelope` passes
- [ ] `cargo test --test envelope_oracle` passes
- [ ] `cargo clippy -p hares-envelope -- -D warnings` passes
