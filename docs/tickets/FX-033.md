---
id: FX-033
title: Medium water heater fixes batch
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-equipment/src/water_heater/gas.rs
  - crates/hares-equipment/src/water_heater/heat_pump_wh.rs
  - crates/hares-equipment/src/water_heater/mod.rs
  - crates/hares-equipment/src/water_heater/tankless.rs
references:
  - vendors/OCHRE/ochre/Models/WaterHeater.py
verification:
  - cargo check --workspace
  - cargo clippy --workspace -- -D warnings
  - cargo test --workspace --exclude hares-python
---

## Background/Context

Batch of medium-severity WH fixes from audit:
- M4: Gas WH default deadband 2°C should be 5.56°C (matching OCHRE)
- M5: HPWH condenser weights only defined for 12-node tanks; validate node count
- M6: `tan(acos(0.0))` → ±Inf when default pf=0 with reactive ZIP config
- M7: Jacket R-value correction omits end-cap area (~17% error)
- M8: Draw profile fraction fallback produces constant rate
- M9: `unmet_load_w` computed but never emitted to ports/telemetry

## Work to Do

- [ ] ~~M4: Change gas.rs deadband~~ **ALREADY CORRECT** — code has `DEFAULT_DEADBAND_C: f64 = 5.555_555_556`. Remove from scope
- [ ] **M5**: Fix HPWH condenser weights for 1- and 2-node cases matching OCHRE's `[1]` and `[0,1]` arrays. For 3-11 nodes, emit `tracing::warn!` and use single-node-midpoint fallback (do NOT hard-error)
- [ ] **M6**: Guard `tan_phi` in `WaterHeaterZip::apply` (mod.rs) with threshold `1e-6` (not `f64::EPSILON`). This shared location covers all WH types — no separate `tankless.rs` fix needed
- [ ] ~~M7: Jacket area end caps~~ **CURRENT CODE IS CORRECT** — residential tank jackets are lateral-sleeve products (bottom inaccessible, top obstructed by T&P valve/pipes/controls). OCHRE and OpenStudio-HPXML both use lateral-only `π×d×h`. No change needed
- [ ] **M8**: Add `tracing::warn!` when constant-rate draw fallback activates
- [ ] **M9**: HPWH already emits `unmet_load_w`. Gas/resistance always return 0 (no TMV). Low value to add constant-zero field — clarify intent or defer
- [ ] Add tests for M5, M6

## Files to Touch

- `crates/hares-equipment/src/water_heater/heat_pump_wh.rs`: fix 1/2-node condenser weights
- `crates/hares-equipment/src/water_heater/mod.rs`: guard ZIP tan_phi, warn on draw fallback

## Measures of Success

- [ ] HPWH 1/2-node condenser weights match OCHRE
- [ ] ZIP reactive power is finite for all valid PF values including 0.0
- [ ] Draw fallback produces a warning

## Implementation Guidance

- **OCHRE reference**: Spot-check the reference implementation in `vendors/OCHRE/` (especially `ochre/models/`, `ochre/utils/`, `ochre/Equipment/`) to verify behavior, edge cases, and default values.
- **Authoritative sources**: Use web search to verify constants, formulas, and algorithms against authoritative references (ASHRAE Fundamentals, EnergyPlus Engineering Reference, NREL technical reports, NIST CODATA) rather than trusting any single implementation.
- **Better physics wins**: If HARES already has better physics than OCHRE, keep it and document the divergence. Only align with OCHRE where OCHRE is correct and HARES is wrong.

## Verification

- [ ] `cargo check --workspace` passes
- [ ] `cargo clippy --workspace -- -D warnings` passes
- [ ] `cargo test --workspace --exclude hares-python` passes
