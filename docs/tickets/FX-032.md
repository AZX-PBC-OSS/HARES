---
id: FX-032
title: Medium HVAC fixes batch
kind: fix
depends_on: [FX-003, FX-004]
files_to_touch:
  - crates/hares-equipment/src/hvac/heat_pump/heater.rs
  - crates/hares-equipment/src/hvac/hvac_core.rs
  - crates/hares-equipment/src/hvac/air_conditioner.rs
references:
  - vendors/OCHRE/ochre/Equipment/HVAC.py
verification:
  - cargo check --workspace
  - cargo clippy --workspace -- -D warnings
  - cargo test --workspace --exclude hares-python
---

## Background/Context

Batch of medium-severity HVAC fixes from audit:
- M1: EIR clamped to [0,1] hides COP<1 misconfig — should validate and error, not silently clamp
- M2: Room AC duct DSE not forced to 1.0 — Room AC has no ducts
- M3: HP min-on/off times default 0.0 vs OCHRE 120/180s

## Work to Do

- [ ] **M1**: Replace `eir.clamp(0.0, 1.0)` with explicit validation: return error when EIR > 1.0 (COP < 1.0) at config-init stage. Note: direct `"eir"` config key inputs bypass this — track separately
- [ ] **M2**: ~~Force Room AC `duct_dse = 1.0`~~ **ALREADY IMPLEMENTED** at `air_conditioner.rs:379`. Add numeric assertion to existing test. No code change needed
- [ ] **M3**: ~~Apply OCHRE defaults 120/180s~~ **OCHRE also defaults to 0** (`Equipment.py:96-99`). The 120/180s are NOT from OCHRE. Either drop M3 or reframe as industry-practice compressor protection sourced from ASHRAE/ACCA
- [ ] Add tests for M1

## Files to Touch

- `crates/hares-equipment/src/hvac/hvac_core.rs`: replace EIR clamp with validation

## Measures of Success

- [ ] COP < 1.0 config produces an error, not silent truncation
- [ ] Room AC duct_dse = 1.0 (already working — add numeric test assertion)

## Implementation Guidance

- **OCHRE reference**: Spot-check the reference implementation in `vendors/OCHRE/` (especially `ochre/models/`, `ochre/utils/`, `ochre/Equipment/`) to verify behavior, edge cases, and default values.
- **Authoritative sources**: Use web search to verify constants, formulas, and algorithms against authoritative references (ASHRAE Fundamentals, EnergyPlus Engineering Reference, NREL technical reports, NIST CODATA) rather than trusting any single implementation.
- **Better physics wins**: If HARES already has better physics than OCHRE, keep it and document the divergence. Only align with OCHRE where OCHRE is correct and HARES is wrong.

## Verification

- [ ] `cargo check --workspace` passes
- [ ] `cargo clippy --workspace -- -D warnings` passes
- [ ] `cargo test --workspace --exclude hares-python` passes
