---
id: CFG-006
title: Cross-crate integration tests for compact ScheduleSource flow
kind: implement
depends_on: [CFG-003, CFG-004, CFG-005]
files_to_touch:
  - crates/hares-io/tests/schedule_integration.rs
references:
  - docs/plans/reflective-mixing-frog.md
verification:
  - cargo test --workspace
  - cargo clippy --workspace
  - cargo bench --bench single_building
---

## Background/Context

After CFG-002 through CFG-005, all equipment reads compact ScheduleSource config keys and all legacy schedule types/functions are deleted. This ticket adds cross-crate integration tests proving the full IO-injection -> equipment-init -> step pipeline works end-to-end.

## Work to Do

- [ ] Add integration tests: IO compact key injection -> ScheduledLoad init -> step for CSV column path
- [ ] Add integration tests: IO compact key injection -> ScheduledLoad init -> step for default profile path
- [ ] Add integration tests: IO compact key injection -> ScheduledLoad init -> step for constant path
- [ ] Add integration tests: IO compact key injection -> EventBasedLoad init -> step with wrap semantics
- [ ] Add integration test: missing column index produces error at init, not at step
- [ ] Verify grep: `rg "schedule_kw_\d|parse_indexed_series|inject_kw_schedule|inject_zero_schedule|inject_event_window|PowerScheduleSource|SetpointSource|WaterHeaterScheduleRefs" crates/` — must return zero results
- [ ] Run full workspace tests + single_building benchmark

## Files to Touch

- `crates/hares-io/tests/schedule_integration.rs`: new cross-crate integration tests

## Measures of Success

- [ ] All integration tests pass end-to-end
- [ ] Grep confirms zero legacy schedule artifacts remain
- [ ] Full test suite passes
- [ ] No performance regression

## Verification

- [ ] `cargo test --workspace` passes
- [ ] `cargo clippy --workspace` passes
- [ ] `cargo bench --bench single_building` no regression
