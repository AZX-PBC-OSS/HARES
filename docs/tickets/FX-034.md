---
id: FX-034
title: Medium physics and IO fixes batch
kind: fix
depends_on: []
files_to_touch:
  - crates/hares-physics/src/psychrometrics.rs
  - crates/hares-physics/src/constants.rs
  - crates/hares-physics/src/solar.rs
  - crates/hares-physics/src/water_mains.rs
  - crates/hares-io/src/epw.rs
  - crates/hares-io/src/schedule_resolve.rs
  - crates/hares-io/src/hpxml/building.rs
references:
  - vendors/OCHRE/ochre/utils/psychrolib_jit.py
  - vendors/OCHRE/ochre/utils/schedule.py
verification:
  - cargo check --workspace
  - cargo clippy --workspace -- -D warnings
  - cargo test --workspace --exclude hares-python
---

## Background/Context

Batch of medium physics/IO fixes:
- M16: Document wet-bulb coefficient divergence (2.381 vs 2.326) in parity test expectations
- M17: Align gas constant to consistent source (287.042 + matching density correction)
- M18: Lower EPW IR fallback threshold from 50 to 1 W/m² (physically justified minimum)
- M19: Document per-curve diffuse IAM divergence from OCHRE's flat 0.854 in parity tests
- M20: Rename water_mains parameter to `dt_peak_to_peak_c` to prevent API misuse
- M21: Parse both supply and return duct leakage from HPXML (not just first value)
- M22: Add `hot_water_clothes_washer` and `hot_water_dishwasher` to schedule column mappings
- M23: Add `ZoneType::Ground` variant to distinguish ground from outdoor air

## Work to Do

- [ ] Add explicit parity-divergence comments in psychrometrics tests for wet-bulb coefficient
- [ ] **M17**: Keep `DRY_AIR_GAS_CONSTANT_J_KG_K = 287.058` (confirmed correct: NIST CODATA 2018 R_universal / ASHRAE M_air=28.966). `HUMIDITY_DENSITY_CORRECTION = 1.607_768_7` is consistent. OCHRE's 287.042 is from PsychroLib with a slightly different M_air basis. Document the divergence
- [ ] **M18**: Lower EPW IR threshold to 1.0 W/m²; add `tracing::warn!` when Clark-Allen fallback activates. **Update existing test** `clark_allen_fallback_activates_when_infrared_below_threshold` (epw.rs:871) — it uses 30.0 which will no longer trigger the fallback. Change test value to < 1.0
- [ ] Add parity-divergence comment in solar.rs for per-curve diffuse IAM
- [ ] Rename `dt_annual_range_c` to `dt_peak_to_peak_c` in water_mains module, update all callers
- [ ] **M21**: In `parse_duct_systems`, split `DuctSystem` struct into `supply_leakage_fraction` and `return_leakage_fraction` fields (matching OCHRE's separate supply/return keys). Parse by iterating `<DuctLeakageMeasurement>` elements and matching on `<DuctType>`
- [ ] **M22**: These columns do NOT go into `COLUMN_MAPPINGS` as `Power`/`EventWindow` — they are handled entirely inside `inject_water_heater_schedule_columns`. The design:
  1. Add two `Ignore` entries to `COLUMN_MAPPINGS` to suppress unknown-column warnings
  2. Compute `avg_water_draw_l_per_day` for `ClothesWasher` and `Dishwasher` in `hpxml/equipment.rs` using OCHRE's ERI formula (inputs already parsed: `RatedAnnualkWh`, `PlaceSettingCapacity`, `LabelUsage`, etc.; requires threading `n_bedrooms` from `Building`)
  3. In `inject_water_heater_schedule_columns`, aggregate all three columns (`hot_water_fixtures` + `hot_water_clothes_washer` + `hot_water_dishwasher`): for each present column, look up its spec's `avg_water_draw_l_per_day`, normalize to L/min, sum into a combined draw vector, inject as `draw_flow_rate_schedule_col`
- [ ] **M23**: Add `ZoneType::Ground` enum variant; update `parse_zone_label`, `zone_key`, `zone_sort_key`, and `infer_exterior_zone` (which currently returns `ZoneType::Outdoor` for slabs). Add test asserting `boundary.exterior_zone == Some(ZoneType::Ground)` for ground-adjacent surfaces
- [ ] Add tests for each change

## Files to Touch

- `crates/hares-physics/src/psychrometrics.rs`: add parity-divergence comments
- `crates/hares-physics/src/constants.rs`: fix gas constant and humidity ratio correction
- `crates/hares-physics/src/solar.rs`: add parity-divergence comment for diffuse IAM
- `crates/hares-physics/src/water_mains.rs`: rename dt_annual_range_c to dt_peak_to_peak_c
- `crates/hares-io/src/epw.rs`: lower IR threshold to 1.0 W/m²
- `crates/hares-io/src/schedule_resolve.rs`: add `Ignore` entries for hot water columns; rewrite `inject_water_heater_schedule_columns` for three-column aggregation
- `crates/hares-io/src/hpxml/equipment.rs`: compute `avg_water_draw_l_per_day` for ClothesWasher and Dishwasher specs
- `crates/hares-io/src/hpxml/building.rs`: parse supply/return duct leakage, add ZoneType::Ground

## Measures of Success

- [ ] Gas constant is from an authoritative source (NIST CODATA or ASHRAE HOF) with documented provenance
- [ ] Duct leakage correctly captures supply and return
- [ ] Ground zones use `ZoneType::Ground`, not `ZoneType::Outdoor`
- [ ] Hot water draw schedule aggregates fixtures + washer + dishwasher into a single combined draw column

## Implementation Guidance

- **OCHRE reference**: Spot-check the reference implementation in `vendors/OCHRE/` (especially `ochre/models/`, `ochre/utils/`, `ochre/Equipment/`) to verify behavior, edge cases, and default values.
- **Authoritative sources**: Use web search to verify constants, formulas, and algorithms against authoritative references (ASHRAE Fundamentals, EnergyPlus Engineering Reference, NREL technical reports, NIST CODATA) rather than trusting any single implementation.
- **Better physics wins**: If HARES already has better physics than OCHRE, keep it and document the divergence. Only align with OCHRE where OCHRE is correct and HARES is wrong.

## Verification

- [ ] `cargo check --workspace` passes
- [ ] `cargo clippy --workspace -- -D warnings` passes
- [ ] `cargo test --workspace --exclude hares-python` passes
